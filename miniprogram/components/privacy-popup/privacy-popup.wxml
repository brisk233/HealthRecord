<view class="pp-mask" wx:if="{{show}}">
  <view class="pp-box">
    <view class="pp-title">用户隐私保护提示</view>
    <view class="pp-content">
      <view class="pp-item"><text class="pp-item-t">📦 我们收集：</text>微信昵称、头像，用于家庭内成员身份展示；openid 用于识别账号并同步家庭数据。</view>
      <view class="pp-item"><text class="pp-item-t">💾 我们使用：</text>仅用于本小程序功能（冰箱库存、菜单、打卡同步），不会用于其他用途；复制邀请码时会写入系统剪贴板（仅写入、不读取）。</view>
      <view class="pp-item"><text class="pp-item-t">🛡️ 拒绝后：</text>仍可浏览与本地使用，仅在用到登录、头像、相册等权限时受限。</view>
    </view>
    <view class="pp-link-line"><button class="pp-link" open-type="openPrivacyContract">查看《用户隐私保护指引》全文</button></view>
    <view class="pp-btns">
      <button class="pp-btn pp-refuse" bindtap="onRefuse">拒绝</button>
      <button class="pp-btn pp-agree" bindtap="onAgree">同意并继续</button>
    </view>
  </view>
</view>