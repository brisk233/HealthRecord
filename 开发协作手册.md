# 欢洋御膳房 · 开发协作手册（项目代号 shilianjia）

> 每轮开工第一件事：读本手册 → 判断本轮任务级别 → 在回复开头标注建议模型 → 对齐进度。

## 一、模型分工规则（核心）

### 原则：Pro 定框架，Flash 干杂活

| 任务类型 | 典型例子 | 用哪个模型 |
| --- | --- | --- |
| 新功能 / 核心逻辑 | 菜单生成引擎、营养计算、云同步、订阅消息 | **Pro** |
| 数据结构改动 | 数据库集合、字段、缓存 key | **Pro** |
| 跨文件重构 | 抽公共组件、换目录结构 | **Pro** |
| 疑难 bug / 审核问题 | 白屏、报错、被驳回 | **Pro** |
| 数据条目维护 | 新增菜谱、改保质期天数、补剂剂量 | Flash |
| 文案 / 样式微调 | 按钮文字、提示语、颜色间距 | Flash |
| 参数微调 | 蛋白质系数、默认份量 | Flash |
| 日常问答 | 怎么用、怎么导入、怎么预览 | Flash |

### 拿不准时的兜底规则
1. 先切 Flash 试一轮（便宜，试错成本低）
2. Flash 做不动 / 开始出逻辑错误 → 立即切回 Pro
3. 任何涉及「云函数、数据结构、算法、跨文件」的改动，直接 Pro，不试 Flash

### 协作流程
1. 用户说明要做的事
2. 模型读本手册 → 判断任务级别 → 回复开头标注【本轮建议模型：Pro / Flash】
3. 用户在 GUI 切换对应模型 → 继续说「继续」
4. 完成后更新本手册「进度」与「任务队列」

## 一.5 QA 铁律（每次开发完成后必须执行）
1. `node tests/run-tests.js` 全量单测必须 0 失败
2. 全页面审计：wxml 事件/字段 vs js 方法/数据 必须全匹配
3. 云函数调用名、require 引用、存储键读写 必须一致
4. **WXML 表达式扫描**：禁用数组/对象字面量（`|| []`、`|| {}`）等编译器非法写法，复杂计算一律放 js 里算好再引用
5. **行为级自测**：对交互逻辑（模块开关/食材删除/习惯更新等）用 mock wx 模拟点击真实执行验证
6. 修复任何失败后重新全量自测，并在此记录结果
7. **Bug 修复标准（v2.3.1 起强制执行）**：
   - 复现第一：动手前先拿到用户精确步骤/页面/登录状态/版本；能复现才修，不能复现先问，绝不盲修
   - 先写失败用例：修复前先加一个能复现该 bug 的自动化测试，确认它红，改到绿
   - 定位层级：区分「没保存 / 保存后被覆盖 / 显示层没刷新 / 事件没触发」，只修根因
   - 验收 = 用户原步骤：用用户原始操作路径重跑新版本，输出前后状态；真机验证不了必须标注「待真机验证」，不得把单测通过冒充已修复
   - 复发即推翻：同一 bug 再报 = 上一轮未到根因，回到复现重查，不在原补丁上叠补丁

> 历史教训：v2.0 启动失败两次——①wxml 标签拼接错位（多出孤儿 </view>）②WXML 表达式使用 `(person.habits || []).length`（WXML 不支持数组字面量）。已加入第 2、4 条强制检查。

## 二、当前进度

- 第 1 周 ✅ 骨架 + 4 核心页面 + 菜谱库（AppID: wx2972567500c1b1fa）
- 第 1 周 ✅ 真实档案已录入：男 172/62 增肌（鱼油+维D+锌）· 女 166/52 塑形（鱼油+铁）· 作息与口味已写入
- 第 2 周 ✅ 菜单引擎 v2（冰箱驱动 + 缺料提示 + 临期优先）+ 营养分析（蛋白/热量/元素/缺口建议）+ 菜谱库 22 道 + 训练课表按真实器械更新
- 第 3 周 ✅ 云开发代码完成（云环境 cloudbase-d1glhkgunaf107df5）：login / syncHome / syncCheckin / remind 四个云函数 + 家庭码双人同步 + 每日 8 点临期定时提醒（待用户按 cloudfunctions/README.md 部署并配置订阅消息模板）
- 第 4 周 ⬜ 真机测试 + 体验版上线

## 三、任务队列（[P]=Pro，[F]=Flash）

### 第 2 周（✅ 已完成）
- [x] 菜单引擎 v2：冰箱库存驱动自动配菜谱 + 缺料提示 + 临期优先
- [x] 营养分析：每餐蛋白/热量估算 + 元素标签 + 按两人目标算缺口与加餐建议
- [x] 菜谱库扩充至 22 道（含蛋白/热量/元素字段）
- [x] 训练课表按真实器械更新（男：引体/高位下拉/划船/哈克深蹲/腿屈伸/蝴蝶机/拳击；女：臀桥机/龙门架/弹力带/普拉提/舞蹈）
- [x] 真实数据全部录入：器械、早餐习惯(外买为主)、主食(米饭)、无固定忙碌日

### 第 3 周（✅ 代码完成，待部署）
- [x] 云开发接入（env: cloudbase-d1glhkgunaf107df5）+ 集合设计（homes / checkins）
- [x] 云函数：login / syncHome / syncCheckin / remind（临期定时 8:00 触发）
- [x] 双人同步：家庭码机制（两人填同一码即同步档案/冰箱/打卡）
- [x] 订阅消息：remind 云函数 + 我的页「开启每日提醒」按钮
- [x] 订阅消息模板已申请（日程提醒），模板 ID 已填入 remind/index.js 与 cloudsync.js
- [x] 4 个云函数已部署（用户确认）
- [x] 修复：第二人填家庭码时先拉后推，避免覆盖第一台数据
- [x] UI 全面美化 v0.3.0：清新薄荷绿主题 · 卡片化 · hero 问候 · 蛋白进度条 · 空状态 · 震动反馈 · 下拉刷新 · emoji 点缀
- [x] 自动化测试：tests/run-tests.js 覆盖 date/shelflife/nutrition/mealplan/recipes/seed 六模块；首轮 15/17，发现并修复 nutrition 性别判断 bug（双人档案改为 key 字段后未同步），复测 18/18
- [x] 小程序正式定名：欢洋御膳房
- [x] v0.4.0 家庭邀请体系：family 云函数（create/join/pull/push/info/setRelation）+ 6 位邀请码 + 关系备注（情侣/夫妻/搭子）+ 微信分享邀请 + 复制邀请码
- [x] v0.4.0 健身独立 Tab：周课表打卡状态 / 今日训练打卡 / 连续打卡天数 / 最近记录 / 动作库（真实器械分组）
- [x] v0.4.0 备菜并入菜单页（顶部切换），tabBar 调整为 今日/菜单/健身/冰箱/我的
- [x] v0.4.1~0.4.2 微信登录精简：首页静默自动登录检测（无弹窗）+ 我的页仅头像（chooseAvatar 点击更换即上传）+ 昵称（type=nickname），家庭卡片显示成员真实头像昵称
- [x] v0.4.3 我的页头像昵称区按用户参考图重排：大圆头像（相机角标）+ 右侧昵称与状态 + 他/她切换，页标题「我的」
- [x] v0.5.0 今日页删除「作息与口味」卡（我的页已有）；健身页新增自定义运动打卡（篮球/羽毛球/其他 + 时长 30/45/60/90 分钟，纳入课表/连续天数/最近记录/云同步）；我的页头像区改紧凑卡片（去大 hero）
- [x] v0.6.0 身份体系：登录后我是我——移除全站他/她切换，性别存家庭成员（family 云函数 setGender）；补剂打卡改为我的页开关+自定义（增删补剂）；我的页新增自动日志（近7天：饮食菜单快照+补剂+运动）；每日提醒置底且开启后显示状态；创建家庭失败提示优化（指向云函数部署）
- [x] v0.6.2 修复：冰箱空时菜单页/今日页空状态提示；创建/加入家庭失败显示真实原因（云函数未部署/环境异常/超时）；取消自定义运动跨设备同步清除；早餐卡补剂文案通用化；我的页头像去阴影（卡片/底圆/角标均无阴影）
- [x] v0.6.3 删除我的页「关于」卡片
- [x] v0.6.4 自动日志支持折叠：默认折叠显示今天 + 本周运动天数，点标题展开近 7 天明细
- [x] v0.6.5 家庭名称支持编辑：创建默认「XX的家」，家庭卡标题 ✏️ 改名（≤12字，云端同步）
- [x] v0.7.0 拖拽排序：健身课表（按人自定义一周顺序，存 profile.trainOrder 云同步）+ 周菜单（拖 ☰ 交换两天菜，存 menuOverride 云同步）；均带「恢复默认」按钮；新增通用 drag 工具
- [x] v0.7.1 家庭名称编辑简化：改名后只保留「保存」按钮（去掉取消）
- [x] v0.7.2 修复家庭名称不生效：本地立即生效 + 云端失败时提示真实原因（若提示「未知 action」= family 云函数旧版，需重新部署 family 含 setName 的版本）
- [x] v0.8.0 大改：菜单引擎重构（只排冰箱真正能做的菜，其余天「需采购食材」占位 + 建议买清单，冰箱与菜单严格同步）；菜单采购清单一键全选；我的页微信登录入口（未登录自动唤起登录提示）+ 退出登录（数据随账号，familyInfo 自动回绑家庭码）+ 头像最左 + 昵称下显示家庭名/未加入 + 补剂卡折叠；支持单人使用（未加入家庭仅本机）
- [x] v0.8.1 测试工程师轮修复 5 项：①拖拽快照带 purchased（把菜拖到占位天显示正确）②启动静默登录后自动回绑家庭并拉数据 ③今日/菜单页 onShow 主动拉云端冰箱与菜单覆盖 ④「重新生成」清除手动覆盖（真·按冰箱重排）⑤头像上传后刷新登录态
- [x] v0.9.0 菜单引擎 v3 完整自动生成：先排冰箱可做的（0 缺料）→ 其余天自动计划菜（缺料最少优先）并列出「买：X」精确买料清单 → 备菜页自动汇总本周采购；7 天永远是具体菜单
- [x] v0.9.1 修复退出登录残留个人信息：未登录时我的页仅显示登录卡（头像/昵称/基础数据/补剂等全部隐藏）；退出清除本机昵称头像；重新登录从家庭成员记录恢复昵称头像（family 云函数 info 新增 myNickname/myAvatarUrl，需重新部署）
- [x] v0.9.2 引擎修正：可做=主菜可做（缺配菜仅提示购买）；晚餐主菜池排除早餐菜；测试期望同步更新
- [x] v0.10.0 作息与喜好可编辑：上班/睡觉时间、口味（清淡/微辣/偏辣）、喜欢食材（多选 chips）实时保存云同步；菜单引擎按喜好自动计算（爱吃优先排、清淡避开辣菜、偏辣多排辣菜，菜谱标注辣度）
- [x] v0.10.1 数据跟账号走：退出登录清空本机全部数据（wx.clearStorageSync + 冰箱置空 + 档案回默认），确认弹窗说明；重新登录自动从云端恢复档案/冰箱/菜单覆盖/昵称头像；家庭数据保留云端
- [x] v0.11.0 家庭权限与交互：基础数据折叠（默认收起，保存后自动折叠）；退出/解散家庭（创建者解散=删除家庭）；仅创建者可邀请/分享/改名，成员显示锁定提示；换头像同步家庭成员（setAvatar）+ 头像加载失败兜底（binderror 显示 👤）；喜欢食材支持自定义添加/点选删除
- [x] v0.11.1 三餐打卡：今日页早/午/晚打卡（按菜单吃/在家做/外卖/聚餐/外食/没吃 六选一），无菜单也能记；打卡云同步（syncCheckin 新增 meals 字段）+ 自动日志晚餐显示实际打卡；健身页同步携带 meals 防覆盖
- [x] v0.11.2 微信隐私合规：新增 privacy-popup 组件（隐私弹窗 + 同意/拒绝 + openPrivacyContract 协议链接）挂载全部页面；头像选择前 requirePrivacyAuthorize 兜底；README 附后台隐私指引配置清单（收集：昵称/头像/openid；第三方：微信云开发）
- [x] v0.11.3 修复家庭成员头像不显示（根治）：family info 接口用管理员权限将成员头像 fileID 转临时链接，绕过云存储「仅创建者可读写」ACL，双方无需改控制台权限即可互看头像
- [x] v0.11.4 隐私授权闭环：privacy-popup 组件升级 ensure(cb)——微信登录/换头像前未授权必先弹隐私弹窗，同意后回调继续执行（通过官方校验模式）；各页面组件加 id；后台指引精简指引（删除未用的手机号/位置/照片视频权限）
- [x] v0.11.5 冰箱入库分类新增：鸡蛋（15天）、其他（7天），remind 临期规则同步
- [x] v0.12.0 严格库存驱动：菜单只排冰箱主料+配菜齐全的菜，缺食材绝不出现该菜（占位「需采购食材」+建议买）；作息与喜好折叠；喜欢的食材显示 ✕ 删除（含自定义项）；备菜步骤一键全选；按钮文案简化+图标化；输入框文字居中
- [x] v0.12.1 家庭详情卡支持折叠：默认收起显示「成员X · ▾」，点标题展开成员/邀请/退出；改名按钮区做冒泡隔离
- [x] v0.12.2 每次进入「我的」页，所有卡片（家庭/基础数据/补剂/作息喜好/日志）重置为默认折叠
- [x] v2.0.0 大更新：更名「欢洋生活」；新增生活方式标签体系（9 人格大方向 + 60+ 细分标签 + 习惯推荐包，data/lifestyle.js）；引导页 onboarding；习惯打卡（今日页习惯流 + 连续天数🔥 + 云同步 habits 字段）；功能模块开关（三餐计划/运动/补剂，按需显示）；V1 数据全保留
- [x] v2.0.1 修复：按钮全局紧凑（一行文字/自动宽度）；功能模块开关改用状态值（e.detail.value）生效；食材删除改独立 ✕ 按钮；习惯支持从模板池选择添加（TEMPLATE_HABITS 12 项）；profile.js/wxml 整体重写消除结构隐患
- [x] v2.0.2 自动日志：空记录不显示（仅三餐/补剂/运动/习惯有真实打卡才生成日志行），无记录显示提示
- [x] v2.0.3 修复家庭卡折叠角标点击不生效：catchtap 拦截范围缩小到 ✏️/✓ 按钮，角标独立绑定 toggleFamily
- [x] v2.0.4 修复头像更新不生效：上传路径加时间戳（每次新 fileID，避免同路径覆盖导致图片缓存不刷新）；本机与对方（setAvatar + 临时链接）都会立即显示新头像
- [x] v2.0.5 引导页人格大方向选中态：补上 .major/.major.on 样式（绿框+阴影）+ 右上角 ✓ 角标
- [x] v2.0.6/2.0.7 修复：①食材独立 ✕ 删除按钮（catchtap）②提醒说明+重新授权入口（无需新模板）③习惯按标签更新（替换推荐保留自定义）④目标按标签细分 ⑤昵称 blur 云端同步+左移 ⑥**功能模块开关根治**：档案拉取字段级合并（mergeProfile 接入 app/today/meals/doLogin 四处），不再整体覆盖导致开关回弹
- [x] v2.0.8 ①默认模块按标签：未选运动型则不默认开启健身（lifestyle.defaultModulesFor，profile/today/fitness/引导四处接入）②新增行为级自动化测试：模拟点击验证模块开关/食材✕删除/习惯更新/默认模块真实生效（已加入 QA 铁律）
- [x] v2.0.9 我的页头部紧凑化：卡片 padding 缩小、头像 88rpx、昵称紧贴头像左移（margin 8rpx）、退出按钮靠左
- [x] v2.1.0 病根修复：mergeProfile 云端 undefined 字段不得覆盖本地（此前旧云端档案把新增的 habits/modules/favs 全部抹掉，导致习惯不保存/模块开关不生效/食材删除回弹）；家庭折叠箭头加大点击区；订阅状态措辞「已订阅」+ 额度说明
- [x] v2.1.1 食材删除加固：✕ 点击区 36→48rpx；删除/添加立即 toast 反馈（已删除「XX」），结果可见
- [x] v2.1.2 习惯持久化根治：mergeProfile 增加「云端空数组不抹本地非空数据」（habits/favs/modules 三字段）；早期旧版推送过的空数组不再清空用户新添加的数据
- [x] v2.1.3 每日提醒按钮单行：文字缩短为「🔔 开启每日提醒」，全部按钮加 white-space:nowrap 防换行
- [x] v2.1.4 功能模块记忆根治：档案每人分区时间戳 _t（saveProfile/引导时写入）；mergeProfile 按时间戳决定以谁为准——旧云端（无时间戳或更旧）永远无法回退本地新改动，开关设置持久记忆
- [x] v2.1.5 按钮全局美化：居中展示（块级 78% 宽居中）、饱满圆润（竖向加高、字距）、小按钮统一 btn-sm 内联样式
- [x] v2.1.6 功能模块交互强化：关闭的行整体置灰 + 「已关闭」标签 + 标题显示「已开 X/3」+ 开关操作即时 toast（已开启/已关闭「XX」）；onModuleToggle 防御式初始化
- [x] v2.1.7 喜好食材删除三重保障：chip 内联 ✕（点 chip 即删）+ 角标 ✕ + 「清空已选食材」按钮；卡片内常驻状态行「已选 X 种 · 上次操作」让数据变化肉眼可见
- [x] v2.1.8 UI 刷新根治：模块卡/喜好 chips 改为预计算纯展示数据（modRows/favChips），WXML 仅最简渲染（消除表达式求值与陈旧绑定问题）；refresh 整体 try/catch 防崩溃兜底；清空按钮独立成行
- [x] v2.1.9 修复两个顽固 bug（根因+行为级自测）：
  - ①功能模块按钮控制不生效根治：今日页 WXML 直接用 `modules.indexOf(...)` 判断显隐，而 WXML 不支持方法调用 → 开关存了但卡片不隐藏；改为 today.js 预计算 modMeals/modSupps/modFitness 纯布尔标志
  - ②关掉最后一个模块后被重置：`!person.modules.length` 把空数组当「未初始化」→ refresh 补默认值；改为 `Array.isArray` 判断，空数组 = 用户选择全关，profile/today/fitness 三处统一
  - ③删除喜好菜品语义修复：chip 自带「 ✕」文案却是开关逻辑（点已删的灰色 chip 会再加回来）；移除内联 ✕，删除统一走右上角红色 ✕（点 chip=添加/取消），提示文案同步
  - 测试：tests/run-tests.js 新增 6 项行为级自测（mock wx 模拟点击）：模块全关不回弹、食材删除持久化、chip 开关语义、今日页显隐标志、健身页 moduleOff、全量 WXML 禁 indexOf 扫描；22/22 通过，开发者工具实测通过
- [x] v2.2.0 喜好食材「真删除」根治：
  - 新增 per-person `favHidden` 字段：点已选 chip 或 ✕ 后，食材从候选列表彻底消失（此前预设项只是取消选中、chip 还在列表，用户认为删除没生效）
  - ✕ 移入 chip 内部（父子关系 + catchtap），杜绝删除事件与 chip 开关事件互相干扰导致「删了又被加回」
  - onFavToggle 改为单向：点灰色=添加，点已选=删除并隐藏；被删除项不允许 chip 加回（防双触发复活），只能通过输入框重新添加
  - seed 双人档案补 favHidden 空数组；mergeProfile ARR_GUARD 增加 favHidden，云端空数组不抹本地删除记录
  - 测试：23/23 通过（新增 favHidden 持久化/彻底消失/输入框加回/mergeProfile 守卫），开发者工具实测
- [x] v2.2.1 四项修复：
  - ①家庭名称编辑按钮过长：新增专用 .icon-btn（56rpx 圆形图标钮，ghost/solid 两态），替换原来 btn-ghost/btn-primary+btn-sm 的长条按钮
  - ②生活方式修改不持久化：mergeProfile 空数组守卫漏了 tags/subTags，云端旧档案的空数组会把本地刚选的人格标签覆盖；ARR_GUARD 补齐；onboarding 增加 onLoad 回显已存选择，再次进入可直观看到当前值
  - ③添加喜好食材按钮美化：新增 .btn-add-fav（渐变胶囊 + 「➕ 添加」，与输入框同高对齐）
  - ④冰箱过期食材「吃掉了」没反应：进入页面时的云端拉取在本地增删后才返回，会用旧数据把已吃的食材“复活”；pullCloud 增加本地改动时间戳判断，拉取期间本地有改动则以本地为准并重推；eatItem 补成功 toast
  - 测试：26/26 通过（新增 tags/subTags 守卫、onboarding 持久化+回显、冰箱吃掉竞态三项行为级自测）
- [x] v2.2.2 按钮统一改小矩形风格：全局 .btn-primary/.btn-ghost 由 78% 宽长胶囊改为内容宽度小矩形（min-width 220rpx、padding 14/44、圆角 16rpx）；.btn-sm 圆角 12rpx 更紧凑；家庭名称 ✏️/✓ 改为 72x48rpx 小矩形（圆角 12rpx）；「➕ 添加」按钮收窄至 150rpx（圆角 14rpx）
- [x] v2.2.3 审核隐私合规补丁：代码「复制邀请码」调用 wx.setClipboardData 属隐私接口（Clipboard），后台指引未声明会被审核拦截、发布后权限被回收；复制前先走隐私弹窗授权（ensure/requirePrivacyAuthorize），弹窗文案补充剪贴板用途（仅写入不读取），README 指引补充「必须勾选剪贴板 + 保存后先发布指引再提交代码审核」
- [x] v2.2.4 退出登录→重新登录数据丢失根治：退出时本机清空档案回默认（无 _t、数组全空），登录拉云端合并时若云端无 _t（旧云端）会判定「本地优先」，空数组的 tags/habits 不会被填回；mergeProfile 增加「本地未保存过（无 _t）则云端整体为准」分支，含用户主动清空的数组也能恢复；测试运行器改为顺序 await，新增 3 项自测：无 _t 云端恢复、本地已保存空数组不被覆盖、退出→重登端到端恢复；30/30 通过
- [x] v2.2.5 自定义习惯图标输入删除：去掉「图标，如：✍️」输入框，添加习惯时按名称自动匹配 emoji（喝水💧/早睡😴/跑步🏃/读书📖/练字✍️/记账🧾/泡脚🦶/做饭🍳/手机📵 等，未命中默认⭐），新增行为级自测
- [x] v2.3.0 使用建议功能：我的页新增「💡 使用建议」卡（提交建议/查看建议两个入口）；提交页支持文字+最多 3 张图片（相册仅选图，隐私弹窗授权后 chooseMedia，压缩后上传云存储 suggestions/ 目录）；查看页展示建议列表（昵称/性别/时间/文字/图片，云函数管理员转临时链接）；family 云函数新增 suggestAdd/suggestList 并自动建 suggestions 集合（家庭模式按家庭码隔离、单人模式只看自己的）；README 隐私指引补充「相册」勾选；测试 35/35 通过（选图/删图/上传提交/列表拉取/页面注册）
- [x] v2.3.1 全量 QA 轮：生成 tests/功能清单.md、tests/测试用例.md、tests/Bug清单.md；全流程测试 41/41；修复 BUG-001（mergeProfile 丢弃根级 trainOrder 导致健身课表排序丢失，合并时保留根级字段）、BUG-002（冰箱竞态仅本页生效且毫秒时间戳同毫秒失效，改为全局单调序号+推送确认 pushFridge/fridgeSyncClean，冰箱/菜单/今日三页统一）、BUG-003（竞态测试返回形状错误假通过，修正并补跨页用例）
- [x] v2.3.2 「生活方式不持久化」真根因根治（BUG-004）：真实模拟器复现——本地保存/重启都正常，退出重登后云端 profile=null；定位到 family 云函数 push 用普通 update，档案从 null 首次写入对象时报 “Cannot create field 'active' in element {profile: null}”，**档案/冰箱从未同步到云端**；修复为 db.command.set() 整体替换并部署到云环境；真实链路回放通过（修改→推送落盘→重启→退出→重登恢复）；测试后已恢复用户档案与冰箱并同步云端；另新增 QA 铁律第 7 条（Bug 修复标准：复现第一/先写失败用例/只修根因/验收=用户原步骤/复发即推翻）
- [x] v2.3.3/v2.3.4 家庭卡 UI 调整：家庭名称文字拉长（flex 占满+超长省略，修复微信 text 直接做 flex 子项时 ellipsis 错误截断）；✏️/✓ 编辑按钮移除，改为点名称直接进入输入框（失焦/回车保存）；顺带修复微信 button 默认 min-width:184px 钳制导致 .btn-add-fav 实际 184px 宽、以及「成员X·▾」角标 catchtap 双触发问题
- [x] v2.3.6 习惯↔菜单联动 + 首次引导 + 按钮文字截断修复：首次进入（无标签且未跳过）自动跳转选择生活方式页（可暂时跳过，我的页保留入口）；mealplan 新增 lifestyleScore 按人格加权（运动型高蛋白/养生型带蔬菜避辣/吃货型热量元素/懒狗型快手/夜猫型避辣，与口味喜好叠加）；我的习惯新增「重新选择生活方式」，重新选择时替换推荐习惯、保留自定义习惯；修复微信 button 组件样式表 `width:184px` 覆盖类宽度导致长按钮文字截断——按钮类加 `!important` + `display:table` 按内容自适应并居中；新增设计文档 tests/设计-习惯与菜单对应.md；测试 48/48，模拟器实测按钮宽度按内容 233px/93px
- [ ] 用户操作：重新部署 family / syncCheckin / remind 三个云函数（集合已改为云函数自动创建，无需手动建库）+ 上传 0.4.0 体验版 → 双人邀请加入测试
- 第 4 周 ⏳ 真机验收中（v0.4.0）

### 第 4 周
- [P] 真机测试 + 修 bug
- [F] 文案与提示语打磨
- [F] 体验版上传操作指导

### 维护期（上线后，Flash 为主）
- [F] 日常：加菜谱、改参数、回答用法问题
- [P] 新需求：任何功能级迭代

## 四、用户资料

✅ 已全部收到：双人档案（男172/62增肌 · 女166/52塑形）、补剂（他：鱼油+维D+锌；她：鱼油+铁）、口味、作息、训练器械、早餐习惯（外买为主：包子/馒头/玉米）、主食（米饭）、忙碌日（无固定）。

云环境 ID：cloudbase-d1glhkgunaf107df5（已录入代码）。

## 五、项目结构速查

```
shilianjia/
├─ project.config.json     # AppID: wx2972567500c1b1fa
├─ README.md               # 导入与预览步骤
├─ 开发协作手册.md          # 本文件：模型分工 + 进度
├─ miniprogram/
│  ├─ app.js / app.json / app.wxss / sitemap.json
│  ├─ utils/   date.js(日期) shelflife.js(保质期) nutrition.js(目标) mealplan.js(菜单+采购+备菜)
│  ├─ data/    seed.js(默认数据) recipes.js(菜谱库)
│  └─ pages/   today(今日) meals(菜单+备菜) fitness(健身) fridge(冰箱) prep(已并入菜单页) profile(我的)
└─ cloudfunctions/         # 第 3 周接入
```

## 六、本地存储 key 速查

| key | 内容 |
| --- | --- |
| profile | 用户档案（gender/height/weight/goal） |
| fridge | 冰箱库存数组（id/name/cat/box/purchased/note） |
| supps-YYYY-MM-DD | 当日补剂打卡 |
| train-YYYY-MM-DD | 当日训练打卡 |
| purchase-done | 采购清单勾选 |
| prep-done | 备菜步骤勾选 |
| familyCode | 家庭邀请码（创建/加入后写入） |
| nickname | 成员昵称 |

## 七、注意
- Flash 上下文能力弱于 Pro：复杂任务不要在 Flash 下推进，切回 Pro 后先读本手册对齐
- 第 2~4 周以 Pro 为主（都在建框架），Flash 省钱主要发生在维护期
- 纯本地模式运行中，云开发（0 服务器费用）第 3 周才接入
